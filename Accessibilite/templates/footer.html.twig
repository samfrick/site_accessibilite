{% extends 'base.html.twig' %}
{% block footer %}
<footer class="bg-gray-900 text-[var(--color-text)] w-full" aria-label="Pied de page principal">
  <div class="max-w-7xl mx-auto px-6 py-10 grid grid-cols-1 md:grid-cols-3 gap-8">
    
    <!-- Logo / Description -->
    <div>
      <h2 class="text-2xl font-bold text-white">MonSite</h2>
      <p class="mt-3 text-sm text-[var(--color-background)]">
        Un site sur la nourriture du monde, ses recettes et ses traditions culinaires.
      </p>
    </div>

    <!-- Liens utiles -->
    <div class="text-white">
      <h3 class="text-lg font-semibold text-white" id="footer-links-heading">
        Liens utiles
      </h3>
      <nav aria-labelledby="footer-links-heading">
        <ul class="mt-3 space-y-2">
          <li>
            <a href="{{ path('accesibilite') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              Accessibilité
            </a>
          </li>
          <li>
            <a href="{{ path('cgu') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              CGU
            </a>
          </li>
          <li>
            <a href="{{ path('rgpd') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              RGPD
            </a>
          </li>
          <li>
            <a href="{{ path('mentionslegales') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              Mentions Légales
            </a>
          </li>
        </ul>
      </nav>
    </div>

    <!-- Plan du site -->
    <div class="text-white">
      <h3 class="text-lg font-semibold" id="sitemap-heading">
        Plan du site
      </h3>
      <nav aria-labelledby="sitemap-heading">
        <ul class="mt-3 space-y-2">
          <li>
            <a href="{{ path('americain') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              Réunion
            </a>
          </li>
          <li>
            <a href="{{ path('pierogi') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              Pologne
            </a>
          </li>
          <li>
            <a href="{{ path('dahl') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              Ile Maurice
            </a>
          </li>
          <li>
            <a href="{{ path('flams') }}" class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[var(--color-secondary)] rounded">
              France
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </div>

  <!-- Bas de page -->
  <div class="border-t border-[var(--color-secondary)] text-center py-4 text-sm text-[var(--color-background)]">
    © 2025 Salomé FRICK, Célia Hoffmann, Etienne Pongérard, Gruber Marine. Tous droits réservés.
  </div>
</footer>

{% endblock %}